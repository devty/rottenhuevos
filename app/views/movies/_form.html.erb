<%= form_for(@movie) do |f| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "What film are you going to see tonight? " %><br />
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit "Pray", :class => 'btn primary' %>
  </div>
<% end %>
