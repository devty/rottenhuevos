<%= form_for(@manpack) do |f| %>
  <% if @manpack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manpack.errors.count, "error") %> prohibited this manpack from being saved:</h2>

      <ul>
      <% @manpack.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :api_key %><br />
    <%= f.text_field :api_key %>
  </div>
    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
